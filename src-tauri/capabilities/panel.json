{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "panel",
  "description": "Capabilities for popped-out panel windows (label panel-*): core APIs + event IPC + window self-destroy/close + runtime window creation (a popout tab can also be dragged out to spawn another popout — review R4-6; popouts only ever create `#popout` windows of this same app, never external URLs). No folder-picker dialog.",
  "windows": ["panel-*"],
  "permissions": [
    "core:default",
    "core:window:allow-destroy",
    "core:window:allow-close",
    "core:webview:allow-create-webview-window"
  ]
}
