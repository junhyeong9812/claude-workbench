{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "panel",
  "description": "Narrower capabilities for popped-out panel windows (label panel-*): core APIs + event IPC only. No folder-picker dialog and NO runtime window creation — those stay on the main window (least privilege, review R0-5).",
  "windows": ["panel-*"],
  "permissions": ["core:default"]
}
