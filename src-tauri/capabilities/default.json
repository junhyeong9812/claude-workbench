{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window: core APIs + folder picker dialog + runtime window creation (multiwindow popouts). Popped-out panel windows use the narrower panel.json capability.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "core:webview:allow-create-webview-window",
    "core:window:allow-get-all-windows",
    "core:window:allow-is-visible",
    "core:window:allow-is-minimized"
  ]
}
